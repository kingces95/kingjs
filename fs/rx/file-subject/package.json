{
  "name": "@kingjs/fs.rx.file-subject",
  "version": "1.0.0",
  "description": "Returns a `IGroupedObservable` with `path` for a `Key`  that emits `IGroupedObservable`s with `stats.ino` for `Key` that each emit `stats.ino` whenever the `ctime` changes.",
  "main": "index.js",
  "files": [
    "*.js"
  ],
  "scripts": {
    "test": "node .test",
    "prepublishOnly": "npm .test",
    "build": "cpk && crm && cdj && clk"
  },
  "repository": {
    "type": "git",
    "url": "https://repository.kingjs.net/fs/rx/file-subject"
  },
  "keywords": [],
  "license": "MIT",
  "dependencies": {
    "@kingjs/fs.rx.inode-subject": "latest",
    "@kingjs/rx.distinct-until-changed": "latest",
    "@kingjs/rx.select": "latest"
  },
  "nodeDependencies": [
    "fs"
  ],
  "capitalize": true
}