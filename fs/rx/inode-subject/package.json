{
  "name": "@kingjs/fs.rx.inode-subject",
  "version": "1.0.0",
  "description": "Returns a `IGroupedObservable` with `path` for a `Key`  that emits `IGroupedObservable`s with `stats.ino` for `Key` that each emit `stats.ino` whenever the `ctime` changes.",
  "main": "index.js",
  "files": [
    "*.js"
  ],
  "scripts": {
    "test": "node .test",
    "prepublishOnly": "npm .test",
    "build": "cpk && crm && cdj && clk"
  },
  "repository": {
    "type": "git",
    "url": "https://repository.kingjs.net/fs/rx/inode-subject"
  },
  "keywords": [],
  "license": "MIT",
  "dependencies": {
    "@kingjs/rx.proxy-subject": "latest",
    "@kingjs/rx.subject": "latest"
  },
  "nodeDependencies": [
    "fs"
  ],
  "capitalize": true
}